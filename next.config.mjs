/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'api.solvexabogados.com',
            'localhost',
            'flowbite.com'
        ],
    },
};

export default nextConfig;
